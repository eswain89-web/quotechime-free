{
  "name": "quotechime-free",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free, browser-private quote follow-up sequence generator.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 4420 --bind 127.0.0.1 --directory demo"
  }
}
